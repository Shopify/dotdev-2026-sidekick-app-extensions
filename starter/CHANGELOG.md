# Changelog

## Unreleased — DotDev 2026 Sidekick workshop starter

- Seeded email-marketing app (React Router template) with a synthetic audience
  and campaigns, plus `/api/customers/lapsed`.
- `winback-tools` data extension (scaffold) and `open-campaign` action extension
  (complete) for the workshop's 4 build steps. See `../prompts/`.
